/*public class SpeedUpNote extends SpecialNote {
    private static final int EFFECT_SPEED = 1;
    // should move the 2 below to Accuracy
    private static final int SPEED_UP_SCORE = 15;
    private static final String SPEEDUP_MESSAGE = "Speed Up";

    public SpecialNote(String dir, int startX, int appearanceFrame) {
        super(dir, startX, appearanceFrame);
    }
} */
